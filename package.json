{
  "name": "modelbound-cursor-plugin",
  "version": "0.3.0",
  "description": "ModelBound for Cursor — slash commands for Trust & Safety findings, Skill Development Pipeline, test, benchmark, compare, suggest, versions, restore, diff. Includes pre-skill-write Git hook to snapshot skill files.",
  "license": "MIT",
  "repository": "https://github.com/ModelBound/cursor-plugin",
  "homepage": "https://modelbound.co/open-source",
  "bin": {
    "modelbound-cursor-install": "./scripts/install.mjs"
  },
  "scripts": {
    "install-plugin": "node scripts/install.mjs",
    "hook": "node scripts/pre-skill-write.mjs"
  },
  "engines": { "node": ">=20" },
  "files": [".cursor", "scripts", "README.md", "LICENSE"]
}
