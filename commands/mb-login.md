# /mb-login
Authenticate the ModelBound CLI (device-code flow or paste API key).

Execute:
```bash
node .modelbound/mb.mjs auth login
```

If the user pasted an `mb_live_*` key in chat, rerun with `--api-key`.
