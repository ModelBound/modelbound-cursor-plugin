# /mb-benchmark
Benchmark skill latency. Usage: `/mb-benchmark <skill-file|slug>`.

Execute:
```bash
npx -y @modelbound/cli benchmark --skill "<skill>"
```

Summarize latency metrics. Note: may return `Unauthorized` until Lovable deploys `_actor` forwarding fix.
