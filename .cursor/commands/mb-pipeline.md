# /mb-pipeline
Run the full Skill Development Pipeline. Usage: `/mb-pipeline <skill> [--phases ...] [--wait]`.

Execute: `npx -y @modelbound/cli pipeline run $ARGS --wait`

Summarize each phase; on failure, list findings and propose fixes.
