# /mb-diff
Unified diff between two versions. Usage: `/mb-diff <skill> <from> [to]`.

Parse `<skill> <from> [to]` (default `to`=current) and run:
`npx -y @modelbound/cli skill diff <skill> --from <from> --to <to>`
