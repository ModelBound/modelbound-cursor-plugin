# /mb-benchmark
Head-to-head benchmark. Usage: `/mb-benchmark <skill> <versionA> <versionB>`.

Parse as `<skill> <a> <b>` and run:
`npx -y @modelbound/cli skill benchmark <skill> --a <a> --b <b>`

Surface the winner and the trade-off.
