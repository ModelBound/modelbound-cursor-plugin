# /mb-test
Run the skill test suite. Usage: `/mb-test <skill-file|slug> [--case <id>] [--prompt "..."]`.

Execute:
```bash
npx -y @modelbound/cli test --skill "<skill>" $REST
```

Summarize pass/fail, adherence score, and latency.
