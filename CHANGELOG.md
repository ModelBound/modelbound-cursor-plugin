# Changelog

## [Unreleased]

### Added
- Initial release scaffold.
- Local trust scoring on every `SKILL.md` via `@modelbound/skill-trust`.
- Per-skill token budget badges via `@modelbound/token-budgets`.
- Sync status indicator (read-only) when the workspace repo is connected to ModelBound.
- Duplicate detection across local skills.
- `allowed_tools` audit for risky combinations.
- Device-flow auth via `ModelBound: Connect` and `ModelBound: Sign Out`.
- Anonymous, opt-out usage telemetry via the `extension-telemetry` endpoint.
